# 📊 Configuration Prometheus - Open Community Manager
# Collecte des métriques pour monitoring

global:
  scrape_interval: 15s      # Fréquence de collecte par défaut
  evaluation_interval: 15s  # Fréquence d'évaluation des règles

# Configuration des alertes (optionnel)
alerting:
  alertmanagers:
    - static_configs:
        - targets:
          # - alertmanager:9093

# Règles d'alertes (optionnel)
rule_files:
  # - "rules/*.yml"

# Configuration des cibles à surveiller
scrape_configs:
  # Prometheus lui-même
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  # Backend Flask API
  - job_name: 'opencommunity-backend'
    static_configs:
      - targets: ['backend:5000']
    metrics_path: '/metrics'  # Endpoint métriques Flask
    scrape_interval: 10s

  # Frontend Nginx (si métriques activées)
  - job_name: 'opencommunity-frontend'
    static_configs:
      - targets: ['frontend:80']
    metrics_path: '/metrics'
    scrape_interval: 30s

  # Base de données PostgreSQL (avec postgres_exporter)
  - job_name: 'postgresql'
    static_configs:
      - targets: ['database:5432']
    scrape_interval: 20s

  # Redis Cache
  - job_name: 'redis'
    static_configs:
      - targets: ['redis:6379']
    scrape_interval: 15s

  # Node Exporter pour métriques système (si installé)
  - job_name: 'node-exporter'
    static_configs:
      - targets: ['host.docker.internal:9100']
    scrape_interval: 15s

  # cAdvisor pour métriques containers Docker
  - job_name: 'cadvisor'
    static_configs:
      - targets: ['host.docker.internal:8080']
    scrape_interval: 20s
